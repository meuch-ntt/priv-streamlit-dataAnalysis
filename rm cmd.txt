------------
env created, modules installed, ONLY run app
------------
cd priv*
.venv\Scripts\activate
streamlit run main.py


------------
First Time
------------
// SKIP if you see folder ".venv"
python -m venv .venv

// ALWAYS active venv. Check: in VSC, command line starts with (.venv) 
//if debug needed  for PSSecurityException -> Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned
.venv\Scripts\activate

// if cloned from github with .venv alreay there -> would be corrupted 
// delete it and install it through requirements 
deactivate
Remove-Item -Recurse -Force .venv

python -m venv .venv
.venv\Scripts\activate

// Requirement file is there, install packages as described in requirements file
// packages will be installed inside virtual environment inside project folder - not globally inside computer  
pip install -r requirements.txt

// SKIP, used to generate requirement file
pip install streamlit pandas matplotlib seaborn notebook
pip freeze > requirements.txt

// RUN app
streamlit run main.py